#include <stdio.h>
#include <string.h>

int main() {
    char str[100];
    
    printf("Enter a string: ");
    gets(str);   // for exam purpose

    printf("Length of the string = %d", strlen(str));

    return 0;
}
